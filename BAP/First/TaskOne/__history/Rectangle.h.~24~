//---------------------------------------------------------------------------
#include <vcl.h>
#ifndef RectangleH
#define RectangleH
//---------------------------------------------------------------------------
#endif
class ÑRect
{
	int x1, y1, x2, y2;
	public:
	ÑRectangle(int, int, int, int);
	void hide(TCanvas*);
	void show(TCanvas*);
	void move(TCanvas*, int, int, int, int);
	~ÑRectangle(){};
};
