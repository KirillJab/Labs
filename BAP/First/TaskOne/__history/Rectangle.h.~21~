//---------------------------------------------------------------------------
#include <vcl.h>
#ifndef RectangleH
#define RectangleH
//---------------------------------------------------------------------------
#endif
class ÑRectangle
{
	int x1, y1, x2, y2;
	public:
	ÑRec(int, int, int, int);
	void hide(TCanvas*);
	void show(TCanvas*);
	void move(TCanvas*, int, int, int, int);
	~ÑRectangle(){};
};
