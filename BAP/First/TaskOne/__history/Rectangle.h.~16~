//---------------------------------------------------------------------------
#include <vcl.h>
#ifndef RectangleH
#define RectangleH
//---------------------------------------------------------------------------
#endif
public class Rectangle
{
	int x1, y1, x2, y2;
	public:
	Rectangle(int, int, int, int);
	void hide(TCanvas*);
	void show(TCanvas*);
	void move(TCanvas*, int, int, int, int);
	~Rectangle(){};
};
